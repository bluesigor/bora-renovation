export declare namespace General {
  interface Benefits {
    id: number;
    title: string;
    desc: string;
    icon: string;
  }

  interface Quote {
    name: string;
    number: string;
    email: string;
    textarea: string;
  }

  interface Window {
    email: any;
  }

  interface Review {
    id: number;
    name: string;
    avatar: string;
    desc: string;
  }
}
